import styled from 'styled-components'
import { XL_SIZE, THIRD_COLOR } from '../../../constants/constants'

export const LogoText = styled.a`
	font-size: ${XL_SIZE};
	text-transform: uppercase;
	color: ${THIRD_COLOR};
	cursor: pointer;
`
